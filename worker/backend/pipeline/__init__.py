"""Vendored .insv → LichtFeld pipeline (from simple-360-to-splat).

Invoke the orchestrator as a module so relative imports resolve:
    python -m backend.pipeline.pipeline_lichtfeld FRONT.insv BACK.insv -o OUT
"""
